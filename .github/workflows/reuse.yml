name: REUSE

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  reuse:
    name: License compliance (REUSE 3.3)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: fsfe/reuse-action@bb774aa972c2a89ff34781233d275075cbddf542 # v5
