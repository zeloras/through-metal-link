# SPDX-License-Identifier: Apache-2.0
# Common (simulator + sweep plots) — any computer
# Pinned to match CI (tools/requirements-ci.txt) so local output matches the
# committed figures byte-for-byte.
numpy==2.5.2
matplotlib==3.11.1

# Schematics renderer (hardware/schematics/render_schematics.py)
schemdraw==0.23

# Raspberry Pi only (rig hardware) — uncomment on the Pi:
# spidev
# smbus2